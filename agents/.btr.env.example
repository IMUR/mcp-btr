# BTR Agent Configuration
# Copy to ~/.btr.env and customize for your environment

# SSH target for API access (hostname or IP)
# Use 'localhost' if BTR runs on the same machine
BTR_HOST=localhost

# Tool Selector API port
BTR_UI_PORT=5010

# Gateway port (for reference)
BTR_GATEWAY_PORT=8090

# Optional: SSH user if different from current
# BTR_SSH_USER=myuser
